
<?php
if ($wo['config']['profile_privacy'] == 1) {
  $users = Wo_WelcomeUsers(10,'welcome');
  foreach ($users as $key => $wo['user']) {
  	 $wo['key'] = $key + 1;
  	 echo Wo_LoadPage('welcome/welcome-users-list');
  }
}
?>
